{
  "name": "zoho-cliq-sentiment-bot",
  "version": "1.0.0",
  "description": "Zoho Cliq extension for real-time employee sentiment analysis and mood tracking",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "npm run test:unit && npm run test:integration",
    "test:unit": "echo \"TODO: Add unit tests for handlers\" && exit 0",
    "test:integration": "echo \"TODO: Add integration tests for webhook flows\" && exit 0",
    "lint": "eslint .",
    "validate": "node -e \"require('./utils/api_client'); console.log('✅ Validation passed')\""
  },
  "keywords": [
    "zoho",
    "cliq",
    "sentiment-analysis",
    "chatbot",
    "employee-wellness"
  ],
  "author": "Your Organization",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "node-fetch": "^2.7.0",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "eslint": "^8.50.0"
  },
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  }
}
